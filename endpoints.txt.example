# API Endpoints — copy this file to endpoints.txt and fill in real values.
# endpoints.txt is .gitignored; never commit credentials to the repository.

# Base API URL
API_URL: https://your-api.example.com/api

# Example endpoint paths (append to API_URL)
# /services/app/DiscretionaryWindow/GetEvalMeth?projType=2&focusId=XXX&critId=XXX
# /services/app/DiscretionaryWindow/GetFocusArea?projType=2
# /services/app/DiscretionaryWindow/GetAdminCrit?projType=2&focusId=XXX
# /services/app/DiscretionaryWindow/GetProjectType
# /services/app/DiscretionaryProject/GetOrgProjects?OrganisationId=XXX

# Test account (fill in locally — never commit real credentials)
# test_email=tester@example.com
# test_password=ChangeMe!
